import styled from "styled-components";

const Wrapper = styled.div`
    
`;

const CreateBlock = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;
`;

const Btns = styled.div`
    display: flex;
    justify-content: center;
    gap: 1.5rem;
`;

export default {
    Wrapper, CreateBlock, Btns
}
